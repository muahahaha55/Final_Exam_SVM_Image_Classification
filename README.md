# Final_Exam_SVM_Image_Classification
This project classifies shell and pebble images using PCA and SVM. PCA reduces image dimensionality while retaining key features, and the resulting representations are used to train an SVM that learns an optimal decision boundary between the two classes.
